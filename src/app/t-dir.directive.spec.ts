import { TDirDirective } from './t-dir.directive';

describe('TDirDirective', () => {
  it('should create an instance', () => {
    const directive = new TDirDirective();
    expect(directive).toBeTruthy();
  });
});
