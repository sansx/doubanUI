import { ModalSwitchDirective } from './modal-switch.directive';

describe('ModalSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
